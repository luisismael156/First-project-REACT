import React,{Fragment} from 'react';


function Formulario({title,description}) {
    return (
      <Fragment>
    <h1 className="text-center">{
      title
    }</h1>
    </Fragment>
    );
  }
 
export default Formulario;